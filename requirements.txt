# Pinned dependency versions. When porting to Cloudera AI, match these in the CML
# runtime so the loaded model and pipeline behave identically to training.

# Data and modeling
numpy==2.4.6
pandas==3.0.3
pyarrow==24.0.0
scikit-learn==1.9.0
xgboost==3.2.0
duckdb==1.5.3

# Serving
fastapi==0.136.3
uvicorn==0.49.0
pydantic==2.13.4

# Shared utilities
python-dotenv==1.2.2
joblib==1.5.3
requests==2.34.2
